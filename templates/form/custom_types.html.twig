

{# templates/form/custom_types.html.twig #}
{% block die_roll_row %}
<div class="form-group">
    <div class="die-roll-field">
        {{ form_label(form) }}
        {{ form_help(form) }}
        <table class="table">
            <tbody>
                <tr>
                    <th>Number of Die</th>
                </tr>
                <tr>
                    <td>
                        {{ form_widget(form.x) }}
                        {{ form_help(form.x) }}
                        {{ form_errors(form.x) }}
                    </td>
                </tr>
                <tr>
                    <th>Die Sides</th>
                </tr>
                <tr>

                    <td>
                        {{ form_widget(form.d) }}
                        {{ form_help(form.d) }}
                        {{ form_errors(form.d) }}
                    </td>
                </tr>
                <tr>
                    <th>Plus</th>
                </tr>
                <tr>

                    <td>
                        {{ form_widget(form.p) }}
                        {{ form_help(form.p) }}
                        {{ form_errors(form.p) }}
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
{% endblock %}


{% block clue_row %}
    ROW
{% endblock %}
